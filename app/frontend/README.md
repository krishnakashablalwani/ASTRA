# CampusHive Frontend

React app for CampusHive. Handles user interface, routing, and theming.

## Setup

```bash
cd frontend
npm install
npm start
```

- The app will run on http://localhost:3000 by default.
